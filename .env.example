VITE_API_BASE_URL=/api
VITE_BACKEND_URL=http://127.0.0.1:8000
# A porta 10000 e do container/deploy. No fluxo local com `php artisan serve`, use 8000.
# IMPORTANTE (Vercel): em producao, use URL completa da API (nao use /api)
# Exemplo: VITE_API_BASE_URL=https://seu-backend.onrender.com/api
